import sqlparse

class SQLQueryRewriter:

    '''
    TODO: Add in additional helper methods for parsing SQL Queries
    '''

    def apply_row_level_security(query):
        '''
        Returns a new query with RLS applied to it
        '''
        pass

    def create_new_repo(query):
        '''
        Rewrites the repo creation query to create it inside a single database (rather than having different repo_bases) 
        '''
        pass